// フォーマッティングによりトークン間の空白が調整されることを確認する。

	// '=' の前後に空白が入る。
	a=0

	// '+' の前後に空白が入る。
	x=x+1
	x=(x+1)*3/2

	// カンマの後ろに空白が入る。
	iota=0,1,2
	screen x,y,z
	a(x,y,z) = f(x,y), g(z,w)

	// カッコの内側の空白が削除される。
	a(  1  )++

	// ラベルの '*' と名前の間の空白が削除される。
* @

	// すでにある空白は削除されない。
	aaa     =    1,  -2
	bbbbbb  =  999,   3

	// ++の前後の空白は調整されない。
	i++
	i ++
