// 使用例

#include "hsp3utf.as"
#include "vartype_int64.hsp"

	mes {"vartype("int64") = "} + vartype("int64")
	assert(int64 == vartype("int64"))

	// 2^60 を計算して表示
	n = int64(2)
	repeat 60
		n *= 2
	loop

	mes str(n)
	logmes str(n)
	assert str(n) == "2305843009213693952"
