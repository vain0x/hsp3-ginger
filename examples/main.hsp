#include "sub.hsp"

	a = 1
	メッセージ = "これは日本語デス"

	repeat 1000000
		redraw 2
		color 255, 255, 255
		boxf
		color
		pos 0, 0
		mes "cnt = " + cnt
		mes "stat = " + stat
		redraw

		sub_run

		await 100
		a += 1
	loop
